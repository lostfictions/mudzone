overwrite: true
schema: "${URL}"
generates:
  packages/server/src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      contextType: ../types/ResolverContext#ResolverContext
  ./graphql.schema.json:
    plugins:
      - "introspection"
