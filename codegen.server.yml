overwrite: true
schema: "${URL}"
generates:
  packages/server/src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
    config:
      mappers:
        Entity: ../store/db-types#EntityDbObject
      scalars:
        DateTime: Date
      contextType: ../resolver-context#ResolverContext
  ./graphql.schema.json:
    plugins:
      - "introspection"
